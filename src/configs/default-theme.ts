type TypeDefaultTheme = {
  colors : {
    backgroundHeader : string;
    backgroundScreen : string;
    primaryText : string;
    secondaryText : string;
    icon : string;
    button : string;
    backgroundComponents : string;
  }
}

export const defaultTheme : TypeDefaultTheme = {
  colors: {
    backgroundHeader : "#202024",
    backgroundScreen : "#121214",
    primaryText : "#FFFFFF",
    secondaryText : "#E1E1E6",
    icon : "#FFFFFF",
    button : "#5C59F6",
    backgroundComponents : "#202024",
  }
}